Table search
This is used by the frontend API to search table information.
---
tags:
  - 'search'
parameters:
  - name: query_term
    in: query
    type: string #keep me... there is a bug in flasgger v-0.9.3 that causes API requests to fail without type on top level
    schema:
      type: string
    required: true
  - name: page_index
    in: query
    type: integer
    schema:
      type: integer
      default: 0
    required: false
  - name: index
    in: query
    type: string
    schema:
      type: string
      default: 'table_search_index'
    required: false
responses:
  200:
    description: table result information
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/SearchTableResults'
